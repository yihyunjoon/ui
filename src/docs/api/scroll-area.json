[
  {
    "name": "ScrollArea",
    "props": [
      {
        "name": "overflowEdgeThreshold",
        "type": "number | Partial<{ xStart: number; xEnd: number; yStart: number; yEnd: number; }> | undefined",
        "required": false,
        "default": "0",
        "description": "The threshold in pixels that must be passed before the overflow edge attributes are applied.\nAccepts a single number for all edges or an object to configure them individually."
      },
      {
        "name": "ref",
        "type": "React.Ref<HTMLDivElement> | undefined",
        "required": false,
        "default": "—",
        "description": "Allows getting a ref to the component instance.\nOnce the component unmounts, React will set `ref.current` to `null`\n(or call the ref with `null` if you passed a callback ref)."
      },
      {
        "name": "id",
        "type": "string | undefined",
        "required": false,
        "default": "—",
        "description": "Unique identifier used to associate labels and descriptions."
      },
      {
        "name": "children",
        "type": "React.ReactNode",
        "required": false,
        "default": "—",
        "description": "Content rendered inside this component."
      },
      {
        "name": "onFocus",
        "type": "((event: BaseUIEvent<React.FocusEvent<HTMLDivElement, Element>>) => void) | undefined",
        "required": false,
        "default": "—",
        "description": "Called when the control receives focus."
      },
      {
        "name": "onBlur",
        "type": "((event: BaseUIEvent<React.FocusEvent<HTMLDivElement, Element>>) => void) | undefined",
        "required": false,
        "default": "—",
        "description": "Called when the control loses focus."
      },
      {
        "name": "onChange",
        "type": "((event: BaseUIEvent<React.ChangeEvent<HTMLDivElement, Element>>) => void) | undefined",
        "required": false,
        "default": "—",
        "description": "Called when the underlying form control changes; inspect the event for its value."
      },
      {
        "name": "onKeyDown",
        "type": "((event: BaseUIEvent<React.KeyboardEvent<HTMLDivElement>>) => void) | undefined",
        "required": false,
        "default": "—",
        "description": "Called when a key is pressed while this element is focused."
      },
      {
        "name": "onClick",
        "type": "((event: BaseUIEvent<React.MouseEvent<HTMLDivElement, MouseEvent>>) => void) | undefined",
        "required": false,
        "default": "—",
        "description": "Called when the element is activated."
      },
      {
        "name": "className",
        "type": "string | ((state: ScrollAreaRootState) => string | undefined) | undefined",
        "required": false,
        "default": "—",
        "description": "CSS class applied to the element, or a function that\nreturns a class based on the component's state."
      },
      {
        "name": "render",
        "type": "React.ReactElement<unknown, string | React.JSXElementConstructor<any>> | ComponentRenderFn<HTMLProps, ScrollAreaRootState> | undefined",
        "required": false,
        "default": "—",
        "description": "Allows you to replace the component's HTML element\nwith a different tag, or compose it with another component.\n\nAccepts a `ReactElement` or a function that returns the element to render."
      },
      {
        "name": "style",
        "type": "React.CSSProperties | ((state: ScrollAreaRootState) => React.CSSProperties | undefined) | undefined",
        "required": false,
        "default": "—",
        "description": "Style applied to the element, or a function that\nreturns a style object based on the component's state."
      }
    ]
  },
  {
    "name": "ScrollBar",
    "props": [
      {
        "name": "orientation",
        "type": "\"horizontal\" | \"vertical\" | undefined",
        "required": false,
        "default": "\"vertical\"",
        "description": "Whether the scrollbar controls vertical or horizontal scroll."
      },
      {
        "name": "keepMounted",
        "type": "boolean | undefined",
        "required": false,
        "default": "false",
        "description": "Whether to keep the HTML element in the DOM when the viewport isn't scrollable."
      },
      {
        "name": "ref",
        "type": "React.Ref<HTMLDivElement> | undefined",
        "required": false,
        "default": "—",
        "description": "Allows getting a ref to the component instance.\nOnce the component unmounts, React will set `ref.current` to `null`\n(or call the ref with `null` if you passed a callback ref)."
      },
      {
        "name": "id",
        "type": "string | undefined",
        "required": false,
        "default": "—",
        "description": "Unique identifier used to associate labels and descriptions."
      },
      {
        "name": "children",
        "type": "React.ReactNode",
        "required": false,
        "default": "—",
        "description": "Content rendered inside this component."
      },
      {
        "name": "onFocus",
        "type": "((event: BaseUIEvent<React.FocusEvent<HTMLDivElement, Element>>) => void) | undefined",
        "required": false,
        "default": "—",
        "description": "Called when the control receives focus."
      },
      {
        "name": "onBlur",
        "type": "((event: BaseUIEvent<React.FocusEvent<HTMLDivElement, Element>>) => void) | undefined",
        "required": false,
        "default": "—",
        "description": "Called when the control loses focus."
      },
      {
        "name": "onChange",
        "type": "((event: BaseUIEvent<React.ChangeEvent<HTMLDivElement, Element>>) => void) | undefined",
        "required": false,
        "default": "—",
        "description": "Called when the underlying form control changes; inspect the event for its value."
      },
      {
        "name": "onKeyDown",
        "type": "((event: BaseUIEvent<React.KeyboardEvent<HTMLDivElement>>) => void) | undefined",
        "required": false,
        "default": "—",
        "description": "Called when a key is pressed while this element is focused."
      },
      {
        "name": "onClick",
        "type": "((event: BaseUIEvent<React.MouseEvent<HTMLDivElement, MouseEvent>>) => void) | undefined",
        "required": false,
        "default": "—",
        "description": "Called when the element is activated."
      },
      {
        "name": "className",
        "type": "string | ((state: ScrollAreaScrollbarState) => string | undefined) | undefined",
        "required": false,
        "default": "—",
        "description": "CSS class applied to the element, or a function that\nreturns a class based on the component's state."
      },
      {
        "name": "render",
        "type": "React.ReactElement<unknown, string | React.JSXElementConstructor<any>> | ComponentRenderFn<HTMLProps, ScrollAreaScrollbarState> | undefined",
        "required": false,
        "default": "—",
        "description": "Allows you to replace the component's HTML element\nwith a different tag, or compose it with another component.\n\nAccepts a `ReactElement` or a function that returns the element to render."
      },
      {
        "name": "style",
        "type": "React.CSSProperties | ((state: ScrollAreaScrollbarState) => React.CSSProperties | undefined) | undefined",
        "required": false,
        "default": "—",
        "description": "Style applied to the element, or a function that\nreturns a style object based on the component's state."
      }
    ]
  }
]
